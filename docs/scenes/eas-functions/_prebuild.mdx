import { GithubIcon } from '@expo/styleguide-icons/custom/GithubIcon';

import { BoxLink } from '~/ui/components/BoxLink';

| 属性            | 类型      | 描述                                                                                                                                        |
| --------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `clean`         | `boolean` | 可选属性，用于定义函数运行命令时是否应使用 `--clean` 标志。默认为 false。                                                                  |
| `apple_team_id` | `string`  | 可选属性，用于定义执行预构建时应使用的 Apple 团队 ID。使用凭据进行 iOS 构建时必须指定此属性。 |

<BoxLink
  title="eas/prebuild 源代码"
  description="在 GitHub 上查看 eas/prebuild 函数的源代码。"
  Icon={GithubIcon}
  href="https://github.com/expo/eas-cli/blob/main/packages/build-tools/src/steps/functions/prebuild.ts"
/>
