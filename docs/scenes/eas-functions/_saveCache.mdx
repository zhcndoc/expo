import { GithubIcon } from '@expo/styleguide-icons/custom/GithubIcon';

import { BoxLink } from '~/ui/components/BoxLink';
import { YesIcon } from '~/ui/components/DocIcons';

| 属性 | 类型 | 必填 | 描述 |
| -------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `key` | `string` | <YesIcon /> | 保存缓存所使用的缓存键。你可以使用 `${{ hashFiles('package-lock.json') }}` 这样的表达式，根据文件哈希创建动态键。此键应与恢复缓存时使用的键一致。 |
| `path` | `string` | <YesIcon /> | 应缓存的目录或文件的路径。此路径应与恢复缓存时使用的路径一致。 |

<BoxLink
  title="eas/save_cache 源代码"
  description="在 GitHub 上查看 eas/save_cache 函数的源代码。"
  Icon={GithubIcon}
  href="https://github.com/expo/eas-cli/blob/main/packages/build-tools/src/steps/functions/saveCache.ts"
/>
