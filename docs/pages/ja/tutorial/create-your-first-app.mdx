---
title: 创建你的第一个应用
description: 本章将学习如何创建一个新的 Expo 项目。
hasVideoLink: true
---

import { Download03Icon } from '@expo/styleguide-icons/outline/Download03Icon';

import { BoxLink } from '~/ui/components/BoxLink';
import { Collapsible } from '~/ui/components/Collapsible';
import { ContentSpotlight } from '~/ui/components/ContentSpotlight';
import { Prerequisites, Requirement } from '~/ui/components/Prerequisites';
import { ProgressTracker } from '~/ui/components/ProgressTracker';
import { Terminal } from '~/ui/components/Snippet';
import { Step } from '~/ui/components/Step';
import { CODE } from '~/ui/components/Text';
import { VideoBoxLink } from '~/ui/components/VideoBoxLink';

在这一章中，我们来学习如何创建并运行一个新的 Expo 项目。

<VideoBoxLink
  videoId="m1-bc53EGh8"
  title="观看：创建你的第一个通用 Expo 应用"
  description="从零开始创建一个新的 Expo 项目，并让它在 Android、iOS 和 web 上运行。"
/>

---

<Prerequisites>
  <Requirement title="在真机上使用 Expo Go">
    在 Android 或 iOS 真机上安装 [Expo Go](https://expo.dev/go)。
  </Requirement>
  <Requirement title="Node.js (LTS)">
    在你的电脑上安装 [Node.js (LTS 版本)](https://nodejs.org/en)。
  </Requirement>
  <Requirement title="代码编辑器">
    安装 [VS Code](https://code.visualstudio.com/) 或你偏好的代码编辑器或 IDE。
  </Requirement>
  <Requirement title="可使用终端的开发机器">
    一台可以在 macOS、Linux 或 Windows（PowerShell 和 WSL2）上打开终端窗口的机器。
  </Requirement>
</Prerequisites>

本教程假定你已经熟悉 TypeScript 和 React。如果还不熟悉，可以先查看 [TypeScript 手册](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html) 和 [React 官方教程](https://react.dev/learn)。

<Step label="1">

## 初始化一个新的 Expo 应用

我们将使用 [`create-expo-app`](/more/create-expo/) 来初始化一个新的 Expo 应用。这是一个用于创建新的 React Native 项目的命令行工具。现在，在终端中运行下面的命令。

<Terminal
  cmd={{
    npm: [
      '# 创建一个名为 StickerSmash 的项目',
      '$ npx create-expo-app@latest StickerSmash',
      '',
      '# CLI 会提示你选择模板。请选择 SDK 54。',
      '$ Select an Expo SDK version > SDK 54',
      '',
      '# 进入项目目录',
      '$ cd StickerSmash',
    ],
    yarn: [
      '# 创建一个名为 StickerSmash 的项目',
      '$ yarn create expo-app StickerSmash',
      '',
      '# CLI 会提示你选择模板。请选择 SDK 54。',
      '$ Select an Expo SDK version > SDK 54',
      '',
      '# 进入项目目录',
      '$ cd StickerSmash',
    ],
    pnpm: [
      '# 创建一个名为 StickerSmash 的项目',
      '$ pnpm create expo-app StickerSmash',
      '',
      '# CLI 会提示你选择模板。请选择 SDK 54。',
      '$ Select an Expo SDK version > SDK 54',
      '',
      '# 进入项目目录',
      '$ cd StickerSmash',
    ],
    bun: [
      '# 创建一个名为 StickerSmash 的项目',
      '$ bun create expo StickerSmash',
      '',
      '# CLI 会提示你选择模板。请选择 SDK 54。',
      '$ Select an Expo SDK version > SDK 54',
      '',
      '# 进入项目目录',
      '$ cd StickerSmash',
    ],
  }}
/>

此命令会使用 [默认](/more/create-expo/#--template) 模板创建一个名为 StickerSmash 的新项目目录。该模板包含构建应用所需的基础样板代码和库，包括 Expo Router。我们会在教程过程中按需添加库。

> **info** 目前，`create-expo-app@latest` 会要求你选择一个 Expo SDK 模板。由于本教程是为 SDK 54 设计的，因此请务必选择 SDK 54 模板。

<Collapsible summary="使用默认模板的好处">

- 创建一个已安装 `expo` 包的全新 React Native 项目
- 包含 Expo CLI 等推荐工具
- 包含 Expo Router 的标签页导航器，用于提供基础导航系统
- 自动配置为可在 Android、iOS 和 web 等多个平台上运行项目
- 默认配置 TypeScript

</Collapsible>

</Step>

<Step label="2">

## 下载资源

<BoxLink
  title="下载资源归档"
  Icon={Download03Icon}
  description="这些资源会在整个教程中使用。"
  href="/static/images/tutorial/sticker-smash-assets.zip"
/>

下载归档后：

1. 解压归档，并替换 **your-project-name/assets/images** 目录中的默认资源。
2. 在代码编辑器或 IDE 中打开项目目录。

</Step>

<Step label="3">

{/* vale off */}

## 运行 reset-project 脚本

{/* vale on */}

在本教程中，你将从零开始构建应用，并学习添加基于文件的导航的基础知识。为了移除样板代码，让我们运行 `reset-project` 脚本。

<Terminal
  cmd={{
    npm: ['$ npm run reset-project'],
    yarn: ['$ yarn run reset-project'],
    pnpm: ['$ pnpm run reset-project'],
    bun: ['$ bun run reset-project'],
  }}
/>

运行上述命令后，**app** 目录中只会保留 2 个文件（**index.tsx** 和 **\_layout.tsx**）。**app** 目录以及其他目录（**components**、**constants**、**hooks** &mdash; 包含样板代码）中的旧文件都会被脚本移动到 **app-example** 目录中。随着教程推进，你会逐步创建自己的目录和组件文件。

<Collapsible summary={<><CODE>reset-project</CODE> 脚本会做什么？</>}>

`reset-project` 脚本会重置项目中 **app** 目录的结构，并将原本位于项目根目录中的旧样板文件复制到一个名为 **app-example** 的单独子目录中。由于它不属于主应用结构的一部分，因此可以删除。

</Collapsible>

</Step>

<Step label="4">

## 在移动端和 web 上运行应用

在项目目录中，运行以下命令，从终端启动 [开发服务器](/more/glossary-of-terms/#development-server)。

<Terminal
  cmd={{
    npm: ['$ npx expo start'],
    yarn: ['$ yarn expo start'],
    pnpm: ['$ pnpm expo start'],
    bun: ['$ bun expo start'],
  }}
/>

运行上述命令后：

1. 开发服务器会启动，并在终端窗口中显示一个二维码。
2. 扫描该二维码即可在设备上打开应用。在 Android 上使用 Expo Go > **Scan QR code** 选项，在 iOS 上使用默认相机应用。
3. 要运行 web 应用，请在终端中按 <kbd>W</kbd>。web 应用会在默认的 web 浏览器中打开。

当它能在所有平台上运行时，应用看起来会是这样。

<ContentSpotlight
  alt="在所有平台上运行的应用"
  src="/static/images/tutorial/01-app-running-on-all-platforms.webp"
  className="max-w-[720px]"
/>

</Step>

<Step label="5">

{/* vale off */}

## 编辑 index 屏幕

{/* vale on */}

**app/index.tsx** 文件定义了应用屏幕中显示的文本。这是应用的入口点，会在开发服务器启动时运行。为了显示背景和文本，我们会使用 React Native 的 `<View>` 和 `<Text>` 等核心组件。

这些组件所应用的样式使用的是 JavaScript 对象，而不是 web 中使用的 CSS。不过，如果你以前在 web 上使用过 CSS，很多属性会让你感到熟悉。大多数 React Native 组件都接受 `style` 属性，并且该属性的值是一个 JavaScript 对象。更多信息请参见 [Styling in React Native](https://reactnative.dev/docs/style)。

让我们修改 **app/index.tsx** 屏幕。

1. 从 `react-native` 中导入 `StyleSheet`，并创建一个定义自定义样式的 `styles` 对象。
2. 为 `<View>` 添加 `styles.container.backgroundColor` 属性，并将值设为 `#25292e`。这将更改背景颜色。
3. 将 `<Text>` 的默认值替换为“Home screen”。
4. 为 `<Text>` 添加 `styles.text.color` 属性，并将值设为 `#fff`（白色），以更改文本颜色。

{/* prettier-ignore */}
```tsx app/index.tsx|collapseHeight=340
import { Text, View, /* @tutinfo 导入 `StyleSheet` 用于定义样式。 */ StyleSheet/* @end */ } from 'react-native';

export default function Index() {
  return (
    <View style={styles.container}>
      /* @tutinfo 之前显示的是 "Edit app/index.tsx to edit this screen"，现在显示为 "Home screen"。 */
      <Text style={styles.text}>Home screen</Text>
      /* @end */
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    /* @tutinfo 将 `backgroundColor` 属性的值设置为 `'#25292e'`。*/
    backgroundColor: '#25292e',
    /* @end */
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    color: '#fff',
  },
});
```

> React Native 使用与 web 相同的颜色格式。它支持十六进制三位缩写（即 `#fff` 这种形式）、`rgba`、`hsl`，以及 `red`、`green`、`blue`、`peru`、`papayawhip` 等命名颜色。更多信息请参见 [Colors in React Native](https://reactnative.dev/docs/colors)。

保存更改后，正在连接到开发服务器的运行中应用会接收到这些更改并应用它们。

<ContentSpotlight
  alt="文本和背景色已更新的应用"
  src="/static/images/tutorial/02-index-screen-changes.webp"
  className="max-w-[720px]"
/>

</Step>

## 总结

<ProgressTracker
  currentChapterIndex={0}
  name="GET_STARTED"
  chapterTitle="第 1 章：创建你的第一个应用"
  summary="我们创建了一个新的 Expo 项目，并使用了 React Native 的核心组件。现在你已经准备好开始开发 StickerSmash 应用了。"
  nextChapterDescription="在下一章中，你将学习如何为应用添加栈导航和标签导航。"
  nextChapterTitle="添加导航"
  nextChapterLink="/ja/tutorial/add-navigation/"
/>
