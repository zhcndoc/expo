---
title: SwiftUI
sidebar_title: SwiftUI
description: 用于使用 @expo/ui 构建原生 iOS 界面的 SwiftUI 组件。
sourceCodeUrl: 'https://github.com/expo/expo/tree/main/packages/expo-ui'
packageName: '@expo/ui'
platforms: ['ios', 'tvos']
isBeta: true
hasVideoLink: true
---

import { BookOpen02Icon } from '@expo/styleguide-icons/outline/BookOpen02Icon';

import { APIInstallSection } from '~/components/plugins/InstallSection';
import { BoxLink } from '~/ui/components/BoxLink';
import { ContentSpotlight } from '~/ui/components/ContentSpotlight';
import { Tabs, Tab } from '~/ui/components/Tabs';
import { CODE } from '~/ui/components/Text';
import { VideoBoxLink } from '~/ui/components/VideoBoxLink';

> **重要** **此库目前处于 beta 阶段，可能会有破坏性变更。** 它在 Expo Go 应用中不可用 &mdash; 请使用[开发构建](/develop/development-builds/introduction/)来试用。

`@expo/ui/swift-ui` 中的 SwiftUI 组件允许你使用 React Native 通过 SwiftUI 构建完全原生的 iOS 界面。

## 安装

<APIInstallSection />

## 用法

使用 `@expo/ui/swift-ui` 中的组件需要将其包装在 [`Host`](#host) 组件中。`Host` 是 SwiftUI 视图的容器。

```tsx
import { Host, Button } from '@expo/ui/swift-ui';

export function SaveButton() {
  return (
    <Host style={{ flex: 1 }}>
      <Button variant="default">保存更改</Button>
    </Host>
  );
}
```

有关 `Host` 工作原理的深入说明，请参阅以下资源：

<BoxLink
  title="Expo UI guide for Swift UI"
  description={
    <>
      了解 <CODE>@expo/ui/swift-ui</CODE> 的基础知识
    </>
  }
  href="/guides/expo-ui-swift-ui/"
  Icon={BookOpen02Icon}
/>

<VideoBoxLink
  videoId="2wXYLWz3YEQ"
  title="Expo UI iOS Liquid Glass 教程"
  description="了解如何使用新的 Expo UI 在你的 React Native 应用中构建真正的 SwiftUI 视图。"
/>

## 组件

### BottomSheet

<Tabs>

<Tab label="iOS">
  <ContentSpotlight
    alt="iOS 上的 BottomSheet 组件。"
    src="/static/images/expo-ui/bottomsheet/ios.png"
    className="max-w-[240px]"
  />
</Tab>

  <Tab label="Code">
    ```tsx
    import { BottomSheet, Host, Text } from '@expo/ui/swift-ui';
    import { useWindowDimensions } from 'react-native';

    const { width } = useWindowDimensions();

    <Host style={{ position: 'absolute', width }}>
      <BottomSheet isOpened={isOpened} onIsOpenedChange={e => setIsOpened(e)}>
        <Text>你好，世界！</Text>
      </BottomSheet>
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/view/sheet(ispresented:ondismiss:content:))*

  </Tab>
</Tabs>

### Button

> **警告** 无边框变体在 Apple TV 上不可用。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Button 组件。"
      src="/static/images/expo-ui/button/ios.png"
      className="max-w-[480px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Button, Host } from '@expo/ui/swift-ui';

    <Host style={{ flex: 1 }}>
      <Button
        variant="default"
        onPress={() => {
          setEditingProfile(true);
        }}>
        编辑个人资料
      </Button>
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/button)*

  </Tab>
</Tabs>

### CircularProgress

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 CircularProgress 组件。"
      src="/static/images/expo-ui/circularprogress/ios.png"
      className="max-w-[380px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { CircularProgress, Host } from '@expo/ui/swift-ui';

    <Host style={{ width: 300 }}>
      <CircularProgress progress={0.5} color="blue" />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/progressview)*

  </Tab>
</Tabs>

### ColorPicker

> **警告** 此组件在 Apple TV 上不可用。

<Tabs>

<Tab label="iOS">
  <ContentSpotlight
    alt="iOS 上的 ColorPicker 组件。"
    src="/static/images/expo-ui/colorpicker/ios.png"
    className="max-w-[240px]"
  />
</Tab>

  <Tab label="Code">
    ```tsx
    import { ColorPicker, Host } from '@expo/ui/swift-ui';

    <Host style={{ width: 400, height: 200 }}>
      <ColorPicker
        label="选择一种颜色"
        selection={color}
        onValueChanged={setColor}
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/colorpicker)*

  </Tab>
</Tabs>

### ContextMenu

> **注意：** 也称为 **DropdownMenu**。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 ContextMenu 组件。"
      src="/static/images/expo-ui/contextMenu/ios.png"
      className="max-w-[380px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { ContextMenu, Host } from '@expo/ui/swift-ui';

    <Host style={{ width: 150, height: 50 }}>
      <ContextMenu>
        <ContextMenu.Items>
          <Button
            systemImage="person.crop.circle.badge.xmark"
            onPress={() => console.log('Pressed1')}>
            你好
          </Button>
          <Button
            variant="bordered"
            systemImage="heart"
            onPress={() => console.log('Pressed2')}>
            喜欢它
          </Button>
          <Picker
            label="Doggos"
            options={['very', 'veery', 'veeery', 'much']}
            variant="menu"
            selectedIndex={selectedIndex}
            onOptionSelected={({ nativeEvent: { index } }) => setSelectedIndex(index)}
          />
        </ContextMenu.Items>
        <ContextMenu.Trigger>
          <Button variant="bordered">
            显示菜单
          </Button>
        </ContextMenu.Trigger>
      </ContextMenu>
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/view/contextmenu(menuitems:))*

  </Tab>
</Tabs>

### DateTimePicker (date)

> **警告** 此组件在 Apple TV 上不可用。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 DateTimePicker（日期）组件。"
      src="/static/images/expo-ui/datetimepicker/date/ios.png"
      className="max-w-[240px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { DateTimePicker, Host } from '@expo/ui/swift-ui';

    <Host matchContents>
      <DateTimePicker
        onDateSelected={date => {
          setSelectedDate(date);
        }}
        displayedComponents='date'
        initialDate={selectedDate.toISOString()}
        variant='wheel'
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/datepicker)*

  </Tab>
</Tabs>

### DateTimePicker (time)

> **警告** 此组件在 Apple TV 上不可用。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 DateTimePicker（时间）组件。"
      src="/static/images/expo-ui/datetimepicker/time/ios.png"
      className="max-w-[240px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { DateTimePicker, Host } from '@expo/ui/swift-ui';

    <Host matchContents>
      <DateTimePicker
        onDateSelected={date => {
          setSelectedDate(date);
        }}
        displayedComponents='hourAndMinute'
        initialDate={selectedDate.toISOString()}
        variant='wheel'
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/datepicker)*

  </Tab>
</Tabs>

### Gauge

> **警告** 此组件在 Apple TV 上不可用。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Gauge 组件。"
      src="/static/images/expo-ui/gauge/ios.png"
      className="max-w-[240px]"
    />
  </Tab>

  <Tab label="Code">
    ```tsx
    import { Gauge, Host } from "@expo/ui/swift-ui";

    <Host matchContents>
      <Gauge
        max={{ value: 1, label: '1' }}
        min={{ value: 0, label: '0' }}
        current={{ value: 0.5 }}
        color={[
          PlatformColor('systemRed'),
          PlatformColor('systemOrange'),
          PlatformColor('systemYellow'),
          PlatformColor('systemGreen'),
        ]}
        type="circularCapacity"
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/gauge)*

  </Tab>
</Tabs>

### Host

一个允许你在 React Native 中放置其他 `@expo/ui/swift-ui` 组件的组件。它的作用类似于 DOM 中的 [`<svg>`](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Element/svg)、[`react-native-skia`](https://shopify.github.io/react-native-skia/) 中的 [`<Canvas>`](https://shopify.github.io/react-native-skia/docs/canvas/overview/)，其底层使用 [`UIHostingController`](https://developer.apple.com/documentation/swiftui/uihostingcontroller) 在 UIKit 中渲染 SwiftUI 视图。

由于 `Host` 组件是一个 React Native [`View`](https://reactnative.dev/docs/view)，你可以向其传入 [`style`](https://reactnative.dev/docs/style) 属性，或者传入 `matchContents` 属性，使 `Host` 组件匹配内容的大小。

```tsx 包裹在 Host 中的 Button
import { Button, Host } from '@expo/ui/swift-ui';

function Example() {
  return (
    <Host matchContents>
      <Button
        onPress={() => {
          console.log('Pressed');
        }}>
        点击
      </Button>
    </Host>
  );
}
```

```tsx 使用 flexbox 和 VStack 的 Host
import { Button, Host, VStack, Text } from '@expo/ui/swift-ui';

function Example() {
  return (
    <Host style={{ flex: 1 }}>
      <VStack spacing={8}>
        <Text>你好，世界！</Text>
        <Button
          onPress={() => {
            console.log('Pressed');
          }}>
          点击
        </Button>
      </VStack>
    </Host>
  );
}
```

### LinearProgress

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 LinearProgress 组件。"
      src="/static/images/expo-ui/linearprogress/ios.png"
      className="max-w-[380px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { LinearProgress, Host } from '@expo/ui/swift-ui';

    <Host style={{ width: 300 }}>
      <LinearProgress progress={0.5} color="red" />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/progressview)*

  </Tab>
</Tabs>

### List

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 List 组件。"
      src="/static/images/expo-ui/list/ios.png"
      className="max-w-[380px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, List } from '@expo/ui/swift-ui';

    <Host style={{ flex: 1 }}>
      <List
        scrollEnabled={false}
        editModeEnabled={editModeEnabled}
        onSelectionChange={(items) => alert(`已选择项目的索引：${items.join(', ')}`)}
        moveEnabled={moveEnabled}
        onMoveItem={(from, to) => alert(`已将索引 ${from} 处的项目移动到索引 ${to}`)}
        onDeleteItem={(item) => alert(`已删除索引为 ${item} 的项目`)}
        listStyle='automatic'
        deleteEnabled={deleteEnabled}
        selectEnabled={selectEnabled}>
        {data.map((item, index) => (
          <LabelPrimitive key={index} title={item.text} systemImage={item.systemImage} color={color} />
        ))}
      </List>
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/list)*

  </Tab>
</Tabs>

### Picker (segmented)

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Picker 组件。"
      src="/static/images/expo-ui/segmentedPicker/ios.png"
      className="max-w-[480px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, Picker } from '@expo/ui/swift-ui';

    <Host matchContents>
      <Picker
        options={['$', '$$', '$$$', '$$$$']}
        selectedIndex={selectedIndex}
        onOptionSelected={({ nativeEvent: { index } }) => {
          setSelectedIndex(index);
        }}
        variant="segmented"
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/picker#Styling-pickers)*

  </Tab>
</Tabs>

### Picker (wheel)

> **警告** 轮盘变体在 Apple TV 上不可用。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Picker 组件。"
      src="/static/images/expo-ui/menuPicker/ios.png"
      className="max-w-[480px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, Picker } from '@expo/ui/swift-ui';

    <Host style={{ height: 100 }}>
      <Picker
        options={['$', '$$', '$$$', '$$$$']}
        selectedIndex={selectedIndex}
        onOptionSelected={({ nativeEvent: { index } }) => {
          setSelectedIndex(index);
        }}
        variant="wheel"
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/pickerstyle/wheel)*

  </Tab>
</Tabs>

### Slider

> **警告** 此组件在 Apple TV 上不可用。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Slider 组件。"
      src="/static/images/expo-ui/slider/ios.png"
      className="max-w-[480px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, Slider } from '@expo/ui/swift-ui';

    <Host style={{ minHeight: 60 }}>
      <Slider
        value={value}
        onValueChange={(value) => {
          setValue(value);
        }}
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/slider)*

  </Tab>
</Tabs>

### Switch (toggle)

> **注意：** 也称为 **Toggle**。

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Switch 组件。"
      src="/static/images/expo-ui/switch/ios.png"
      className="max-w-[480px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, Switch } from '@expo/ui/swift-ui';

    <Host matchContents>
      <Switch
        checked={checked}
        onValueChange={checked => {
          setChecked(checked);
        }}
        color="#ff0000"
        label="播放音乐"
        variant="switch"
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/toggle)*

  </Tab>
</Tabs>

### Switch (checkbox)

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 Picker 组件。"
      src="/static/images/expo-ui/checkbox/ios.png"
      className="max-w-[480px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, Switch } from '@expo/ui/swift-ui';

    <Host matchContents>
      <Switch
        checked={checked}
        onValueChange={checked => {
          setChecked(checked);
        }}
        label="播放音乐"
        variant="checkbox"
      />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/toggle)*

  </Tab>
</Tabs>

### TextField

<Tabs>
  <Tab label="iOS">
    <ContentSpotlight
      alt="iOS 上的 TextField 组件。"
      src="/static/images/expo-ui/textinput/ios.png"
      className="max-w-[320px]"
    />
  </Tab>
  <Tab label="Code">
    ```tsx
    import { Host, TextField } from '@expo/ui/swift-ui';

    <Host matchContents>
      <TextField autocorrection={false} defaultValue="单行文本输入" onChangeText={setValue} />
    </Host>
    ```
    *另见：[官方 SwiftUI 文档](https://developer.apple.com/documentation/swiftui/textfield)*

  </Tab>
</Tabs>

### More

Expo UI 仍在积极开发中。我们会持续添加更多功能，并且可能会更改 API。文档中的一些示例可能并非最新。如果你想查看最新更改，请查看 [示例](https://github.com/expo/expo/tree/main/apps/native-component-list/src/screens/UI)。

## API

完整文档尚未提供。请使用 TypeScript 类型来探索该 API。

```ts
// 从 SwiftUI 包中导入
import { BottomSheet } from '@expo/ui/swift-ui';
```
