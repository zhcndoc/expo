---
title: 路由器原生标签页
description: 一个提供原生标签页布局的 Expo Router 子模块。
sourceCodeUrl: 'https://github.com/expo/expo/tree/sdk-54/packages/expo-router'
packageName: 'expo-router'
platforms: ['android', 'ios', 'tvos', 'web', 'expo-go']
isNew: true
---

import { BookOpen02Icon } from '@expo/styleguide-icons/outline/BookOpen02Icon';

import APISection from '~/components/plugins/APISection';
import { BoxLink } from '~/ui/components/BoxLink';
import { ConfigPluginExample } from '~/ui/components/ConfigSection';

> **重要** Native tabs 是一个 alpha 功能，适用于 SDK 54 及更高版本，其 API 可能会发生变化。

`expo-router/unstable-native-tabs` 是 `expo-router` 的一个子模块，导出用于使用平台原生系统标签页构建标签页布局的组件。

> 有关适用于原生和 Web 应用的基于文件路由库的更多信息，请参阅 [Expo Router](./router) 参考文档。

## 安装

要在你的项目中使用 `expo-router/unstable-native-tabs`，你需要先在项目中安装 `expo-router`。请按照 Expo Router 的安装指南操作：

<BoxLink
  title="安装 Expo Router"
  description="了解如何在你的项目中安装 Expo Router。"
  href="/router/installation/"
  Icon={BookOpen02Icon}
/>

## 在 app 配置中进行配置

如果你使用 [默认](/more/create-expo/#--template) 模板创建新项目，`expo-router` 的 [配置插件](/config-plugins/introduction/) 已经在你的 app 配置中完成配置。

<ConfigPluginExample>

```json app.json
{
  "expo": {
    "plugins": ["expo-router"]
  }
}
```

</ConfigPluginExample>

## 用法

要了解如何在 Expo Router 中使用原生标签页，请阅读原生标签页指南：

<BoxLink
  title="原生标签页"
  Icon={BookOpen02Icon}
  href="/router/advanced/native-tabs/"
  description="了解如何在你的 Expo Router 应用中使用原生标签页。"
/>

## API

```js
import { NativeTabs, Icon, Label, Badge, VectorIcon } from 'expo-router/unstable-native-tabs';
```

<APISection packageName="expo-router-native-tabs" apiName="原生标签页" />
