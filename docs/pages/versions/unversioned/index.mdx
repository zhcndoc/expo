---
title: Expo SDK 参考
description: 使用 Expo SDK 包在你的 Expo 和 React Native 应用中访问设备和系统功能。
hideTOC: true
---

import { BookOpen02Icon } from '@expo/styleguide-icons/outline/BookOpen02Icon';

import { BoxLink } from '~/ui/components/BoxLink';
import { Collapsible } from '~/ui/components/Collapsible';
import RedirectNotification from '~/ui/components/RedirectNotification';
import {
  AndroidIOSCompatibilityTable,
  ReactNativeCompatibilityTable,
} from '~/ui/components/SDKTables';
import { Terminal } from '~/ui/components/Snippet';
import { CODE } from '~/ui/components/Text';

<RedirectNotification>
  You are looking for the page does not exist in this SDK version. It may have been deprecated, or
  added in a newer SDK version.
</RedirectNotification>

Expo SDK is a collection of packages that provide access to device and system features such as camera, contacts, location, sensors, haptics, and more. Each package is dedicated to a specific feature and can be used independently. As long as the `expo` package is installed, all packages will work in any React Native app.

You can use the [`npx expo install`](/more/expo-cli/#install) command to install any Expo SDK package. For example, you can install three different packages using the following command:

<Terminal
  cmd={{
    npm: ['$ npx expo install expo-camera expo-contacts expo-sensors'],
    yarn: ['$ yarn expo install expo-camera expo-contacts expo-sensors'],
    pnpm: ['$ pnpm expo install expo-camera expo-contacts expo-sensors'],
    bun: ['$ bun expo install expo-camera expo-contacts expo-sensors'],
  }}
/>

After installing one or more packages, you can import them into your JavaScript code:

```ts
import { CameraView } from 'expo-camera';
import { Contact } from 'expo-contacts';
import { Gyroscope } from 'expo-sensors';
```

This allows you to write [`Contact.getAll()`](./sdk/contacts#contactgetalloptions) and read contacts from the device, read the gyroscope sensor to detect device movement, or turn on the phone camera and take a photo.

## 所有 Expo SDK 包都可在任何 React Native 应用中使用

Expo 应用本身就是 React Native 应用，因此只要安装并配置了 `expo` 包，所有 Expo SDK 包都可以在任何 React Native 应用中使用。创建支持 Expo SDK 包的 React Native 应用最简单的方法是使用 `create-expo-app`。不过，你也可以使用 `npx install-expo-modules` 命令为现有的 React Native 应用添加 Expo SDK 支持。

<Terminal
  cmd={{
    npm: [
      '# 创建一个名为 my-app 的项目',
      '$ npx create-expo-app my-app --template bare-minimum',
    ],
    yarn: [
      '# 创建一个名为 my-app 的项目',
      '$ yarn create expo-app my-app --template bare-minimum',
    ],
    pnpm: [
      '# 创建一个名为 my-app 的项目',
      '$ pnpm create expo-app my-app --template bare-minimum',
    ],
    bun: ['# 创建一个名为 my-app 的项目', '$ bun create expo my-app --template bare-minimum'],
  }}
/>

<BoxLink
  title="在现有 React Native 应用中安装 Expo SDK 包"
  href="/bare/installing-expo-modules"
  description={
    <>
      了解更多关于将使用{' '}
      <CODE>npx @react-native-community/cli@latest init</CODE> 创建的项目配置为支持 Expo SDK 包。
    </>
  }
  Icon={BookOpen02Icon}
/>

<BoxLink
  title="使用库"
  description="了解如何在你的项目中安装 Expo SDK 包。"
  href="/workflow/using-libraries"
  Icon={BookOpen02Icon}
/>

## 使用预发布版本

新的 Expo SDK 版本每年发布三次。在这些发布之间，我们会发布 `expo` 包以及所有 Expo SDK 包的预发布版本。预发布版本不被视为稳定版本，只有在你能够接受遇到 bug 或其他问题风险时才应使用。

### 金丝雀版本

金丝雀发布代表了它们发布时 `main` 分支状态的一个快照。金丝雀包版本的名称中包含 `-canary`，并带有日期和提交哈希，例如 `56.0.0-canary-20260506-03817f5`。要安装最新的金丝雀版本：

<Terminal
  cmd={{
    npm: [
      '# 安装 expo 及其相关包的金丝雀版本',
      '$ npm install expo@canary && npx expo install --fix',
    ],
    yarn: [
      '# 安装 expo 及其相关包的金丝雀版本',
      '$ yarn add expo@canary && yarn expo install --fix',
    ],
    pnpm: [
      '# 安装 expo 及其相关包的金丝雀版本',
      '$ pnpm add expo@canary && pnpm expo install --fix',
    ],
    bun: [
      '# 安装 expo 及其相关包的金丝雀版本',
      '$ bun add expo@canary && bun expo install --fix',
    ],
  }}
/>

你通常可以在 Expo SDK 的稳定版本中使用单个包的预发布版本。金丝雀质量的发布有时可能会出现不兼容或其他问题。如果你选择使用金丝雀包，并且已经验证它适用于你的使用场景，你可能希望[关闭依赖验证警告](/more/expo-cli/#configuring-dependency-validation)。

### Beta 版本

在每个 Expo SDK 版本发布之前，我们会发布 `expo` 包以及所有 Expo SDK 包的 beta 版本。Beta 版本被认为比金丝雀版本稳定得多，我们鼓励开发者在他们的应用中尝试并分享反馈。Beta 版本在 npm 上使用 `beta` 标签，并遵循相关 [更新日志](https://expo.dev/changelog) 帖子中的说明。

## 每个 Expo SDK 版本都依赖于特定的 React Native 版本

<ReactNativeCompatibilityTable />

### 其他信息

<Collapsible summary="用于跟踪 React Native 发布的 Expo SDK 政策">

- 每个 Expo SDK 版本都面向单一的 React Native 版本。通常这就是发布时的最新稳定版。
- React Native 目前每年发布六个版本，并且目标是让每个版本都不包含破坏性变更。
- Expo SDK 的发布旨在遵循这一节奏。
- 即将发布的 Expo SDK 预发布版本会很快支持最新的 React Native 版本，通常在其发布的当天就会支持。Expo SDK 团队的一名成员会参与每次 React Native 发布团队的工作，负责持续更新 Expo 仓库中的 React Native 版本、验证兼容性，并将回归问题反馈给 React Native 团队。

</Collapsible>

<Collapsible summary="如果我需要最新 React Native 版本中的某个改动，但它还没有出现在 Expo SDK 发布版中，该怎么办？">
Expo 在 React Native 发布团队中的工程师会确保任何紧急修复都包含在最新 Expo SDK 所使用的 React Native 版本中。如果你的特定修复不会被 cherry-pick 到现有版本中，可能是因为它不被视为关键修复，或者因为它涉及破坏性变更，那么你有两个选择：

1. 使用 [`patch-package`](https://github.com/ds300/patch-package) 来引入该修复。
2. 使用 [Expo SDK 的预发布版本](#using-pre-release-versions)。

</Collapsible>

<Collapsible summary="我可以在最新的 Expo SDK 中使用较旧版本的 React Native 吗？">

Expo SDK 中的包旨在支持该 SDK 所对应的 React Native 版本。通常，它们不支持较旧版本的 React Native，但也可能支持。当 React Native 发布新版本时，Expo SDK 包的最新版本通常会更新以支持它。不过，这可能需要几周甚至更久，具体取决于该版本中变更的范围。

</Collapsible>

## Supported Android and iOS Versions

Each version of the Expo SDK supports a minimum version of the Android and iOS operating systems. For Android, `compileSdkVersion` is defined, which tells [Gradle](https://developer.android.com/studio/build) which Android SDK version to use to compile the app. This also means that you can use Android API features included in that SDK version and earlier versions. For iOS, [Xcode](https://developer.apple.com/news/upcoming-requirements/) indicates the minimum Xcode SDK version used when compiling the app.

<AndroidIOSCompatibilityTable />

When deciding whether to upgrade your Expo SDK version, please also consider Expo's SDK version and app store submission requirements, as described in the table above. The Google Play Store and Apple App Store periodically raise their minimum required operating system versions and API levels, which are required for new app submissions. Expo cannot control app store requirements, and you should check [Google](https://developer.android.com/studio/build) and [Apple](https://developer.apple.com/news/upcoming-requirements/) for current store submission requirements.
