---
title: 间隔器
description: 一种布局间隔器，用于在兄弟元素之间产生空白。
sourceCodeUrl: 'https://github.com/expo/expo/tree/main/packages/expo-ui'
packageName: '@expo/ui'
platforms: ['android', 'ios', 'web', 'expo-go']
---

import APISection from '~/components/plugins/APISection';
import { APIInstallSection } from '~/components/plugins/InstallSection';

一种布局间隔组件，用于在 [`Row`](row) 或 [`Column`](column) 内的兄弟元素之间产生空白。使用 [`size`](#size) 创建固定间距，或使用 [`flexible`](#flexible) 填充剩余的主轴空间。

## 安装

<APIInstallSection />

## 用法

### 固定大小的间隔

```tsx FixedSpacerExample.tsx
import { Host, Column, Text, Spacer } from '@expo/ui';

export default function FixedSpacerExample() {
  return (
    <Host matchContents>
      <Column>
        <Text>上方</Text>
        <Spacer size={32} />
        <Text>下方</Text>
      </Column>
    </Host>
  );
}
```

### 可伸缩间隔

可伸缩间隔会填充其父元素主轴方向上的剩余空间，将周围内容推到两端。

```tsx FlexibleSpacerExample.tsx
import { Host, Row, Text, Spacer } from '@expo/ui';

export default function FlexibleSpacerExample() {
  return (
    <Host style={{ flex: 1 }}>
      <Row>
        <Text>前导</Text>
        <Spacer flexible />
        <Text>尾随</Text>
      </Row>
    </Host>
  );
}
```

## API

```tsx
import { Spacer } from '@expo/ui';
```

<APISection packageName="expo-ui/universal/spacer" apiName="Spacer" />
