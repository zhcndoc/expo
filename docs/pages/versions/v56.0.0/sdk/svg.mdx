---
title: react-native-svg
description: 一个允许在你的应用中使用 SVG 的库。
sourceCodeUrl: 'https://github.com/react-native-community/react-native-svg'
packageName: react-native-svg
platforms: ['android', 'ios', 'macos', 'web', 'tvos', 'expo-go']
inExpoGo: true
---

import { BookOpen02Icon } from '@expo/styleguide-icons/outline/BookOpen02Icon';

import { APIInstallSection } from '~/components/plugins/InstallSection';
import { BoxLink } from '~/ui/components/BoxLink';
import { SnackInline } from '~/ui/components/Snippet';
import { MONOSPACE } from '~/ui/components/Text';

`react-native-svg` 允许你在应用中使用 SVG，并支持交互和动画。

## 安装

<APIInstallSection href="https://github.com/react-native-community/react-native-svg#with-react-native-cli" />

## API

```js
import * as Svg from 'react-native-svg';
```

### <MONOSPACE>Svg</MONOSPACE>

一组绘图原语，例如 `Circle`、`Rect`、`Path`、
`ClipPath` 和 `Polygon`。它支持大多数 SVG 元素和属性。
其实现由 [react-native-svg](https://github.com/react-native-community/react-native-svg) 提供，相关文档也在该仓库中。

<SnackInline label='SVG' dependencies={['react-native-svg']}>

```tsx
import Svg, { Circle, Rect } from 'react-native-svg';

export default function SvgComponent(props) {
  return (
    <Svg height="50%" width="50%" viewBox="0 0 100 100" {...props}>
      <Circle cx="50" cy="50" r="45" stroke="blue" strokeWidth="2.5" fill="green" />
      <Rect x="15" y="15" width="70" height="70" stroke="red" strokeWidth="2" fill="yellow" />
    </Svg>
  );
}
```

</SnackInline>

### 提示

这里有一些有用的链接，可以帮助你快速上手！

- 在找 SVG 吗？试试 [Lucide](https://lucide.dev/)。
- 使用 [Figma](https://www.figma.com/) 可以免费创建或修改你自己的 SVG。
- 使用 [SVGOMG](https://jakearchibald.github.io/svgomg/) 优化你的 SVG。这会让代码更小，更易于处理。请务必不要移除 `viewbox`，以在 Android 上获得最佳效果。
- 使用 [SVGR](https://react-svgr.com/playground/?native=true&typescript=true) 在浏览器中将你的 SVG 转换为 Expo 组件。

## 了解更多

<BoxLink
  title="访问官方文档"
  description="获取 API 及其用法的完整信息。"
  Icon={BookOpen02Icon}
  href="https://github.com/software-mansion/react-native-svg"
/>
