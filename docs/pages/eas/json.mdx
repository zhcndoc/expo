---
title: 使用 eas.json 进行配置
description: 了解用于 EAS Build 和 EAS Submit 的可用属性，以便在项目中配置并覆盖其默认行为。
maxHeadingDepth: 4
---

import { EasJsonPropertiesTable } from '~/components/plugins/EasJsonPropertiesTable';
import androidSchema from '~/public/static/schemas/unversioned/eas-json-build-android-schema.js';
import commonSchema from '~/public/static/schemas/unversioned/eas-json-build-common-schema.js';
import iosSchema from '~/public/static/schemas/unversioned/eas-json-build-ios-schema.js';
import submitAndroidSchema from '~/public/static/schemas/unversioned/eas-json-submit-android-schema.js';
import submitIosSchema from '~/public/static/schemas/unversioned/eas-json-submit-ios-schema.js';
import { Collapsible } from '~/ui/components/Collapsible';

**eas.json** 是 EAS CLI 和服务的配置文件。你可以在本页找到 [EAS Build](/build/introduction) 和 [EAS Submit](/submit/introduction) 的所有可用 schema 属性的完整参考。

> **info** 要了解更多关于使用 **eas.json** 配置 EAS 服务项目的信息，请参阅 [使用 eas.json 配置 EAS Build](/build/eas-json/) 和 [使用 eas.json 配置 EAS Submit](/submit/eas-json/)。

## EAS Build

以下属性可用于 **eas.json** 中 `build` 键的 schema。

<Collapsible summary="多个构建配置的示例 schema">

```json eas.json
{
  "build": {
    "base": {
      "node": "12.13.0",
      "yarn": "1.22.5",
      "env": {
        "EXAMPLE_ENV": "example value"
      },
      "android": {
        "image": "default",
        "env": {
          "PLATFORM": "android"
        }
      },
      "ios": {
        "image": "latest",
        "env": {
          "PLATFORM": "ios"
        }
      }
    },
    "development": {
      "extends": "base",
      "developmentClient": true,
      "env": {
        "ENVIRONMENT": "development"
      },
      "android": {
        "distribution": "internal",
        "withoutCredentials": true
      },
      "ios": {
        "simulator": true
      }
    },
    "staging": {
      "extends": "base",
      "env": {
        "ENVIRONMENT": "staging"
      },
      "distribution": "internal",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "extends": "base",
      "env": {
        "ENVIRONMENT": "production"
      }
    }
  }
}
```

</Collapsible>

### 原生平台的通用属性

<EasJsonPropertiesTable schema={commonSchema} />

### Android 特定选项

<EasJsonPropertiesTable schema={androidSchema} />

### iOS 特定选项

<EasJsonPropertiesTable schema={iosSchema} />

## EAS Submit

以下属性可用于 **eas.json** 中 `submit` 键的 schema。

<Collapsible summary="生产配置的示例 schema">

```json eas.json
{
  "cli": {
    "version": ">= 0.34.0"
  },
  "submit": {
    "production": {
      "android": {
        "track": "internal"
      },
      "ios": {
        "appleId": "john@turtle.com",
        "ascAppId": "1234567890",
        "appleTeamId": "AB12XYZ34S"
      }
    }
  }
}
```

</Collapsible>

### Android 特定选项

<EasJsonPropertiesTable schema={submitAndroidSchema} />

### iOS 特定选项

<EasJsonPropertiesTable schema={submitIosSchema} />
