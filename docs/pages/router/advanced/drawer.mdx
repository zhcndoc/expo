---
title: 抽屉
description: 了解如何在 Expo Router 中使用抽屉布局。
---

import { ContentSpotlight } from '~/ui/components/ContentSpotlight';
import { Terminal } from '~/ui/components/Snippet';
import { Tabs, Tab } from '~/ui/components/Tabs';

导航抽屉是移动应用中的一种常见模式，它允许用户从屏幕一侧滑出菜单，以显示导航选项。这个菜单通常也可以通过应用程序标题栏中的按钮进行切换。

<ContentSpotlight
  alt="展示抽屉导航实际效果的视频。"
  file="expo-router/drawer.mp4"
/>

## 安装

在 **SDK 56 及更高版本** 中，抽屉导航器已打包在 `expo-router` 中，并在底层使用 [`react-native-drawer-layout`](https://www.npmjs.com/package/react-native-drawer-layout)。

在 Android 和 iOS 上，抽屉需要 `react-native-reanimated` 和 `react-native-worklets` 来驱动其动画。在 web 上，动画由 CSS 处理。

<Tabs>

<Tab label="SDK 56 and later">

要使用抽屉导航器，如果你还没有安装这些依赖，请安装它们：

<Terminal
  cmd={[
    '$ npx expo install react-native-reanimated react-native-worklets react-native-gesture-handler',
  ]}
/>

</Tab>

<Tab label="SDK 54 and 55">

要使用 [drawer navigator](https://reactnavigation.org/docs/drawer-navigator)，如果你还没有安装这些依赖，请安装它们：

<Terminal
  cmd={[
    '$ npx expo install @react-navigation/drawer react-native-reanimated react-native-worklets react-native-gesture-handler',
  ]}
/>

</Tab>

<Tab label="SDK 53 and earlier">

要使用 [drawer navigator](https://reactnavigation.org/docs/drawer-navigator)，如果你还没有安装这些依赖，请安装它们：

<Terminal
  cmd={[
    '$ npx expo install @react-navigation/drawer react-native-reanimated react-native-gesture-handler',
  ]}
/>

</Tab>

</Tabs>

## 用法

现在你可以使用 `Drawer` 布局来创建一个抽屉导航器。

```tsx src/app/_layout.tsx
import { Drawer } from 'expo-router/drawer';

export default function Layout() {
  return <Drawer />;
}
```

要编辑抽屉导航菜单的标签、标题和屏幕选项，需要为特定屏幕按如下方式进行设置：

```tsx src/app/_layout.tsx
import { Drawer } from 'expo-router/drawer';

export default function Layout() {
  return (
    <Drawer>
      <Drawer.Screen
        name="index" // 这是页面的名称，必须与根路径中的 url 匹配
        options={{
          drawerLabel: 'Home',
          title: 'overview',
        }}
      />
      <Drawer.Screen
        name="user/[id]" // 这是页面的名称，必须与根路径中的 url 匹配
        options={{
          drawerLabel: 'User',
          title: 'overview',
        }}
      />
    </Drawer>
  );
}
```
